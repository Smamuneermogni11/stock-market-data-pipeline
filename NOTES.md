# 🧠 My Data Engineering Notes

## Git Commands
git status
git add .
git commit -m "message"
git push

git add .
git commit -m "update"
git push

---

## Python Commands
source venv/bin/activate
deactivate
pip install pandas
pip install yfinance

---

## SQL Basics
SELECT * FROM table;
SELECT AVG(column);